apiVersion: v1
kind: Service
metadata:
  name: mcpo-service
  namespace: mcpo
  labels:
    app: mcpo
spec:
  selector:
    app: mcpo
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    name: http
  type: LoadBalancer
